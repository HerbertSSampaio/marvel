import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
    root: {
        marginTop: 5,
        marginBottom: 10,
        padding: 20,
    },
    header: {
        marginTop: 20,
        backgroundColor: "#424242",
        borderRadius: 10,
    },
    characterImage: {
        margin: "auto",
    },
    session: {
        marginTop: 30,
        marginBottom: 60,
        padding: 40,
    },
});