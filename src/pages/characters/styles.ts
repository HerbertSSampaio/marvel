import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
    root: {
        marginTop: 40,
        marginBottom: 10,
        padding: 40,
    },
    header: {
        backgroundColor: "#424242",
        borderRadius: 10,
    },
    session: {
        marginTop: 30,
        marginBottom: 60,
        padding: 40,
    },
});